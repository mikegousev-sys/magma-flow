# Скоростемер потока никелевого расплава — состояние проекта

Документ для передачи в новый чат. Приложите его вместе с последними файлами
кода (см. список ниже) первым сообщением.

## 1. Что это за проект

Система измеряет скорость потока расплава в жёлобе и уровень расплава,
бесконтактно, тремя независимыми способами:
- **фотодиоды** (4 шт, корреляционный метод) — основной канал скорости;
- **камера** (профиль яркости вдоль потока) — независимая проверка скорости
  + измерение уровня по геометрии зеркала;
- **стереопара камер** (триангуляция) — ещё одна независимая проверка обеих
  величин, готова как модуль, но пока не подключена к общему процессу.

Железо: Arduino Nano (фотодиоды) → Raspberry Pi 5 (сервер) → клиент.
Отдельно — Raspberry Pi + камера MV-MIPI-SC130M → процесс зрения → свой клиент.

## 2. Архитектура: ядро и дополнение

Проект разделён на две категории по требованию пользователя:

**Ядро — трогать только в исключительных случаях, с явным обоснованием:**
`magma_server.cpp`, `magma_signal.hpp`, `magma_speed.ino`, `magma_blob.hpp`
(неиспользуемый черновик блоб-трекинга, оставлен как есть), `magma_client.cpp`,
`magma_client_logging.cpp`, `magma_protocol.hpp`, `magma_logging.hpp`.

**Дополнение — редактируется свободно:**
`magma_capture.hpp`, `magma_veye.hpp`, `magma_exposure.hpp`, `magma_edge.hpp`,
`magma_level.hpp`, `magma_flow.hpp`, `magma_config.hpp`, `magma_broadcast.hpp`,
`magma_vision_protocol.hpp`, `magma_vision.cpp`, `magma_vision_client.cpp`,
`magma_stereo.hpp`, плюс пример настроек `magma_vision.conf`.

Ключевой архитектурный принцип: **зрение — полностью самостоятельный процесс**,
свой протокол (NDJSON), свой порт (9100), свой broadcaster. Ядро не затронуто
ни одной правкой ради зрения — это осознанный выбор, а не недосмотр.

## 3. Ключевые физические параметры (определены в разговоре)

- Геометрия жёлоба: дуга R150 мм, рабочий максимум уровня 105 мм.
- Расстояния между фотодиодами вдоль потока: 75, 50, 75 мм (6 баз всего).
- Камера для скорости/уровня: MV-MIPI-SC130M, глобальный затвор, 1280×1024.
- Угол камеры к плоскости расплава β ≈ 31° (определён по опорному снимку
  через две линии известной длины — вдоль и поперёк потока).
- Масштаб при объективе 30 мм: ≈0.4135 мм/пиксель на дистанции 4 м
  (пересчитано 2026-09-14 с исходных ≈0.248 мм/px при 50 мм — масштаб
  обратно пропорционален фокусному расстоянию при той же дистанции и
  матрице; изменено в `magma_vision.conf`, `magma_vision.cpp` и
  `magma_stereo.hpp`, где заодно поправлен `focal_length_mm`).
- Стереопара: база 125 мм, **вдоль потока** (не поперёк — важное уточнение,
  изменившее формулы проекции в magma_stereo.hpp).
- Диафрагма F/6, расплав ярче ожидаемого — насыщение матрицы наступает
  за десятки-сотни микросекунд, смаз от движения почти всегда пренебрежим.

## 4. Ключевые алгоритмические решения и их причины

- **Многобазовая корреляция** (и для фотодиодов, и для камеры): перебор
  скоростей-кандидатов, вклады со всех баз/интервалов складываются ДО поиска
  пика. Устраняет проскок цикла и ложный пик на нулевой задержке — обе
  проблемы реально наблюдались на архивных данных.
- **GCC-PHAT** вместо обычной корреляции — устойчивее к доминирующей
  синхронной помехе.
- **Вычитание неподвижного фона** обязательно для камеры — без него
  корреляция цепляется за неподвижную корку на стенках и даёт ноль.
- **Медиана**, а не среднее, для сглаживания секундных/серийных оценок —
  устойчива к единичным выбросам.
- **Горячая перезагрузка настроек** (`magma_config.hpp`) — все параметры
  наладки (полосы выборки, метки, углы, границы выдержки) меняются без
  перезапуска процесса зрения; кроме адреса/порта вещания (требует
  перезапуска, это единственное осознанное исключение).
- **Адаптивная выдержка** (`magma_exposure.hpp`) — держит настройки при
  пустом жёлобе (иначе первая серия после прихода расплава ушла бы в
  насыщение), приоритет выдержки над усилением до предела по смазу.
- **Триангуляция скорости по стереопаре** — НЕ через 3D, а через разность
  пиксельных диспаратностей метки (неподвижной) и текстуры (движущейся);
  разность делится на Δt между кадрами двух камер. Требует известного Δt —
  метод работает БЛАГОДАРЯ рассинхронизации камер, а не вопреки ей.

## 5. Нерешённый физический вопрос

Истинная скорость потока — **1.5 или 2.9 м/с** — не установлена. Прежние
логи допускали оба ответа из-за слишком большого пятна обзора фотодиодов
(≈120 мм при базах 50–200 мм). Рекомендовано сузить пятно щелевой диафрагмой
и/или сменить оптику. Независимая проверка через расход (сечение жёлоба ×
производительность) не проводилась — данных о производительности не было.

## 6. Статус кода

Все файлы (кроме `.ino` — нет тулчейна в этой среде) собраны и проверены:
`magma_server`, `magma_client`, `magma_client_logging`, `magma_vision`,
`magma_vision_client` — без предупреждений при `-Wall -Wextra -Wpedantic`.
`magma_stereo.hpp` проверен на синтетике (скорость сходится с ошибкой ~0.8%
на диапазоне 0.5–4.5 м/с), но НЕ подключён к `magma_vision.cpp` — это
следующий шаг, если понадобится.

При сборке найдены и исправлены реальные ошибки: перепутанный порядок
аргументов `GccPhat` в стереомодуле (диспаратность была верна по модулю, но
не по знаку), отсутствие метки времени файла при первой загрузке конфига
(ложное «файл изменился»), неверный синтаксис сырых C++-строк в регулярках.

## 7. Текущая задача: схемы алгоритма (по одной на файл, PNG, Graphviz)

Сделаны (готовы, отправлены пользователю): `magma_stereo.hpp`,
`magma_vision_client.cpp`, `magma_vision.cpp`, `magma_vision_protocol.hpp`,
`magma_broadcast.hpp`, `magma_config.hpp`, `magma_exposure.hpp`,
`magma_capture.hpp`, `magma_veye.hpp`, `magma_flow.hpp`.

Осталось (в порядке новизны файла, от новых к старым):
`magma_edge.hpp` (в процессе на момент вопроса), `magma_level.hpp`,
затем ядро в обратном порядке создания: `magma_blob.hpp`,
`magma_client_logging.cpp`, `magma_client.cpp`, `magma_server.cpp`,
`magma_speed.ino`, `magma_logging.hpp`, `magma_protocol.hpp`,
`magma_signal.hpp`.

Инструменты/приёмы, которые сработали: Graphviz (`dot`, splines=spline,
НЕ ortho — не поддерживает подписи рёбер), для независимых веток — два
параллельных «коридора» сверху вниз, для рядов с 4+ независимыми входами —
принудительная раскладка в 2 ряда через `rank=same` + невидимые рёбра
(иначе схема расползается по ширине). После рендера — обязательная проверка
кроп-скриншотами на наложение текста перед отправкой.

## 8. Доступ к железу

- Один из двух Raspberry Pi 5 проекта: hostname `Magma-pie3`, IP
  `192.168.137.165`, пользователь `mike`. Настроен беспарольный SSH-вход
  (ключ ed25519 в `~/.ssh/id_ed25519` на машине разработчика, публичный
  ключ добавлен в `~/.ssh/authorized_keys` на pi). Подключение:
  `ssh mike@192.168.137.165`.
- Роль: на этом pi5 работают ОБА процесса — сервер фотодиодов
  (`magma_server.cpp`) и vision-процесс (`magma_vision.cpp`). Второй Pi5
  проекта, соответственно, скорее всего клиентский/наблюдательный — уточнить
  при необходимости, доступ к нему пока не настроен.

### Настройка среды на Magma-pie3 (выполнено 2026-09-14)

- OS: Debian 13 (trixie), kernel 6.18.34, g++ 14.2.0 (C++20), 2 ГБ RAM.
- Зависимостей от OpenCV/Boost код НЕ требует (только stdlib + Linux V4L2/
  sockets/winsock2 под `#ifdef _WIN32`) — сборка прошла без установки
  дополнительных библиотек.
- Исходники лежат на pi в `~/magma/code/` (скопированы все `.cpp`/`.hpp`/
  `.conf`, кроме `.ino` — тулчейна для Arduino на pi нет и не нужен, он
  прошивается отдельно).
- Все 5 бинарников собраны там же (`g++ -std=c++20 -Wall -Wextra -Wpedantic
  -O2`) без единого предупреждения: `magma_server`, `magma_vision`,
  `magma_client`, `magma_client_logging`, `magma_vision_client`.
- Камера MV-MIPI-SC130M на CAM0: инициализация через
  `~/raspberrypi_v4l2/rpi5_scripts/media_setting_rpi5.sh mvcam -fmt RAW8
  -w 1280 -h 1024 -c 2` (нужен `sudo`; libcamera камеру НЕ видит —
  `rpicam-hello --list-cameras` пуст, это MV-тип, работает напрямую через
  media-ctl/V4L2, не через libcamera). После настройки кадр читается через
  `/dev/video0` (`v4l2-ctl -d /dev/video0 --stream-mmap --stream-count=1
  --stream-to=frame.raw`), формат GREY 1280×1024 (1 310 720 байт).
- Тестовый кадр захвачен и проверен: картинка есть (min=0, max=255,
  среднее≈27 — видна структура, не однородный шум/чернота). Экспозиция при
  тесте не калибровалась под реальный объект — просто подтверждение, что
  сенсор и конвейер V4L2 рабочие.
- Инициализация media-ctl для CAM0 не переживает перезагрузку — при
  следующем сеансе media_setting_rpi5.sh нужно прогонять заново перед
  стартом `magma_vision`.
- Автозапуск при подаче питания настроен через systemd (`enable`d, проверено
  настоящей перезагрузкой): `magma-camera-init.service` (прогоняет
  media_setting_rpi5.sh, root) → `magma-server.service` (порт 9000) →
  `magma-vision.service` (порт 9100, ждёт camera-init). Оба порта вещают
  NDJSON клиенту сразу после подачи питания, без ручного запуска.

### Git (настроено 2026-09-15)

- Репозиторий: https://github.com/mikegousev-sys/magma-flow (приватный/
  публичный — на выбор пользователя, создан вручную через веб-интерфейс).
- Доступ — по SSH-ключу (отдельный, только для GitHub, НЕ тот же ключ, что
  для входа на Pi): `~/.ssh/id_ed25519_github` и на ПК, и на Pi, публичные
  ключи добавлены в https://github.com/settings/keys. `~/.ssh/config` на
  обеих машинах указывает `github.com` использовать этот ключ
  (`IdentitiesOnly yes`, чтобы не мешал ключ для Pi).
- **На ПК** — сама рабочая папка `C:\Users\mikeg\Documents\Расходометрия`
  является репозиторием (`git remote -v` → `origin` = тот же URL).
- **На Pi** — репозиторий отдельно клонирован в `~/magma-flow/` (структура
  папок совпадает с ПК, включая `code/`). Реально работающие/запущенные
  файлы остаются в `~/magma/code/` (пути захардкожены в systemd-юнитах,
  трогать не стал) — это НЕ тот же каталог, что и репозиторий.
- **Рабочий процесс:** правка → коммит → `git push` (с ПК) → на Pi
  `cd ~/magma-flow && git pull`, затем скопировать нужные файлы в
  `~/magma/code/` (`cp ~/magma-flow/code/<файл> ~/magma/code/`) и
  пересобрать/перезапустить сервис при необходимости. Синхронизация
  каталогов НЕ автоматическая — при обновлении кода не забыть этот
  ручной шаг copy после `git pull`.
- `.gitignore` (в корне ПК-репозитория) исключает скомпилированные
  бинарники — они специфичны для платформы (Pi) и пересобираются из
  исходников, хранить их в git смысла нет.

### USB gadget mode (выполнено 2026-09-14/15)

- На плате физически один USB-C — используется одновременно и для питания,
  и (через `dwc2` в peripheral-режиме) для данных. Линии данных на нём
  РАЗВЕДЕНЫ (проверено практически: COM-порт и Ethernet-gadget видны на
  хосте) — уточнение к общему мнению «USB-C Pi5 только для питания».
- **Нельзя** одновременно подавать питание от штатного блока И получать
  VBUS от USB-порта хоста через тот же кабель — конфликт двух источников
  5В вызывает аварийное отключение (красный LED). Питание должно идти
  только с одной стороны.
- Питание с порта хоста работает надёжно только через **задний** USB-C
  порт материнской платы ПК (напрямую на плате) — передний (через шлейф
  на переднюю панель корпуса) не даёт достаточного тока, вызывает
  ту же аварию (недостаточно тока → undervoltage → отключение).
- Настройка (уже применена и переживает перезагрузку):
  - `/boot/firmware/config.txt`: `[pi5]` → `dtoverlay=dwc2,dr_mode=peripheral`;
    `[all]` → `usb_max_current_enable=1` (снимает софт-ограничение на
    ВЫХОДНОЙ ток USB-портов Pi при отсутствии официального PD-блока; НЕ
    влияет на входное питание от хоста).
  - `/boot/firmware/cmdline.txt`: добавлен `modules-load=dwc2,g_ether`
    (сразу после `rootwait`).
  - Резервные копии обоих файлов: `*.bak-pre-gadget` в том же каталоге.
  - `usb-gadget-net.service` (systemd, свой юнит, НЕ NetworkManager —
    NM ждёт carrier и не поднимает интерфейс надёжно при загрузке):
    поднимает `usb0` и назначает статический `169.254.84.1/16` при каждом
    старте.
  - Windows-хост сам получает адрес автоконфигурации в той же
    169.254.0.0/16 подсети (APIPA) — админ-прав на стороне Windows не
    требуется.
- Подключение с ПК: физически кабель в **задний** USB-C порт материнки,
  затем `ssh mike@169.254.84.1`.

### Инцидент 2026-09-15: камера пропала после манипуляций с питанием/кабелями

- После серии сегодняшних отключений/переподключений камера перестала
  находиться (`mvcam 10/11 NOT FOUND`, I2C шины 10 и 11 пустые,
  `magma-vision` получал `errors:1, frames:0` на каждой серии).
  `vcgencmd get_throttled` показывал `0x50005` — было и историческое, и
  ТЕКУЩЕЕ undervoltage (бит 0).
- Причина: камеру физически переставили **с CAM0 на CAM1** во время осмотра
  шлейфа. `magma-camera-init.service` при загрузке не успевал её найти —
  гонка: I2C-пробинг сенсора после power-up происходит с задержкой, скрипт
  проверяет оба порта один раз и не переповторяет попытку.
- Диагностика: `media-ctl -d /dev/media1 -p` показал полностью собранную
  цепочку `mvcam 11-003b` → `csi2`, хотя `i2cdetect -y 11` в этот момент
  показывал только `UU` (занято драйвером, не отвечает на пробу) — то есть
  ядро уже держит устройство, просто наш скрипт проверял раньше, чем
  завершилась инициализация сенсора.
- Исправление (временное, вручную): `sudo systemctl stop magma-vision`
  (иначе `VIDIOC_S_FMT: Device or resource busy`) → повторный запуск
  `media_setting_rpi5.sh mvcam -fmt RAW8 -w 1280 -h 1024 -c 2` → `sudo
  systemctl start magma-vision`. После этого захват пошёл сразу
  (`frames:120, errors:0`).
- **Исправлено 2026-09-15:** `magma-camera-init.service` теперь запускает
  `~/magma/camera-init-retry.sh` — до 6 попыток с паузой 3с, пробует оба
  порта (`-c 2`) каждый раз, завершается успешно по фразе `set CAM. finish`
  в выводе скрипта; если камера не найдена после всех попыток — юнит всё
  равно завершается успешно (exit 0), чтобы не блокировать
  `magma-vision.service` (тот сам переоткрывает `/dev/video0` перед каждой
  следующей серией, см. `EnsureCameraOpen()` в magma_vision.cpp).
  Проверено настоящей холодной перезагрузкой: попытка 1 не нашла камеру
  (та самая гонка), попытка 2 (через 3с) нашла на CAM1 и настроила —
  `magma-vision` заработал сразу, без ручного вмешательства.
- **Найден и исправлен отдельный баг того же дня:** `veye_mvcam` скрипт
  (`mv_mipi_i2c_new.sh`) вызывает свои бинарники (`i2c_4write` и т.п.) по
  ОТНОСИТЕЛЬНОМУ пути `./имя` — то есть относительно текущего рабочего
  каталога ПРОЦЕССА, а не каталога самого скрипта. Раз `magma_vision`
  запущен из `~/magma/code/`, эти вызовы падали с «No such file or
  directory», а сам bash-скрипт при этом завершался с кодом 0 (без
  `set -e` код ошибки внутренней команды не пробрасывается) — то есть
  `VeyeControl::Apply()` тихо думал, что выдержка применена (`ok=true`,
  `exposure_us=0`), хотя I2C-запись не проходила вообще. Экспозиция была
  фактически неуправляемой с момента переноса на pi. Исправлено в
  `magma_veye.hpp::Command()` — перед запуском скрипта явно `cd` в его
  каталог. Заодно исправлен `veye.i2c_bus` в конфиге (10 → 11, камера
  сейчас на CAM1). Проверено: `exposure_us`/`gain_db` в отчёте теперь
  реальные, меняются между сериями.

### Вторая камера — задел под стереопару (добавлено 2026-09-15)

- В `magma_vision.cpp`/`magma_vision.conf` добавлена ПОЛНОСТЬЮ независимая
  вторая камера: свой `Camera`, свой `VeyeControl` (`veye2.i2c_bus`), своя
  `CameraCalibration` (`calibration2.*`, по умолчанию тот же объектив
  30 мм/F6, но переопределяется отдельно — камеры физически идентичны, но
  оптика на них может отличаться), свой `ExposureController`
  (`exposure2.*` — полностью отдельные пороги/цели регулятора).
- **Выключена по умолчанию** (`camera2.device` пуст) — нулевое влияние на
  текущую работу, собрано и проверено без предупреждений
  (`-Wall -Wextra -Wpedantic`), проверено что при выключенном состоянии
  вторая камера не логирует вообще ничего.
- **Граница того, что сделано:** реализованы только детект/подключение/
  автовыдержка второй камеры — расчёт скорости/уровня по-прежнему ведётся
  ТОЛЬКО по первой. Подключение второго потока к триангуляции из
  `magma_stereo.hpp` НЕ сделано заодно — это отдельная задача (см. п.7),
  которую честно не стал додумывать без второй физической камеры для
  проверки (сейчас на плате есть только одна, её переставляют между CAM0/
  CAM1 при необходимости). Когда стереопара будет физически смонтирована,
  первым делом стоит проверить: (а) реальные `/dev/videoN` для CAM0 и CAM1
  одновременно заняты разными камерами (не проверялось — сейчас единиц
  ственная камера тестировалась поочерёдно на обоих портах, никогда не
  было такого, чтобы обе шины I2C отвечали одновременно); (б) калибровку
  через метки для второй камеры (сейчас только ручной путь, в отличие от
  первой камеры, где есть решатель по трём меткам).

- Проверено остальное дерево проекта на похожие гонки/отсутствие retry:
  сетевые клиенты (`magma_client.cpp`, `magma_client_logging.cpp`,
  `magma_vision_client.cpp`) и UART в `magma_server.cpp` уже имеют
  встроенный retry на переподключение — доработка не нужна. Единственным
  местом без защиты от гонки при холодном старте был именно системный
  скрипт инициализации камеры (теперь исправлено).

### Частота кадров камеры (проверено 2026-09-15)

- Было 60 fps, поднято до **120 fps** (текущее значение в `magma_vision.conf`)
  — сенсор и конвейер захвата держат чисто, `timeouts:0, errors:0`.
- Проверен и более высокий диапазон: 150–200 fps сенсор принимает без
  возражений (`maxfps` при текущей выдержке — 214.85), реально измеренная
  скорость захвата: ~103 fps при цели 120, ~155 fps при цели 200 — тоже
  без единой ошибки. Разрыв цель/факт (~20-25%) стабилен на всех
  значениях, похоже на фиксированный оверхед серии (warmup-кадры), а не
  на потерю кадров. Если нужно 150-200 реально — просто поднять
  `camera.frame_rate` в конфиге, работать будет.

### Расчёт питания на 2 камеры + Arduino + 4 фотодиода (сделан 2026-09-15,
### НЕ проверен физически — второй камеры и Arduino сейчас нет на плате)

- MV-MIPI-SC130M: 1.0 Вт / 5В ≈ 200 мА каждая (datasheet VEYE).
- Pi5 сама плата: 2.1-4.8 Вт в простое, до 8.8-11.6 Вт под полной нагрузкой
  CPU (независимые замеры Tom's Hardware/CNX Software).
- Итог худшего случая: Pi5 (11.6 Вт) + 2 камеры (2.0 Вт) + Arduino Nano по
  USB (~0.5 Вт консервативно) + 4 фотодиода (пассивные, ~0) ≈ **14.1 Вт**
  против бюджета официального БП 27 Вт (5В/5А) — запас ~13 Вт (~48%).
  `usb_max_current_enable=1` уже снят (см. USB gadget mode выше), весь
  бюджет 5А доступен.

### ОТКРЫТЫЙ ВОПРОС: порог по интенсивности АЦП (пользователь поднял
### 2026-09-15, правки НЕ делались — жду уточнения)

Пользователь помнит про порог «~5000 отсчётов АЦП», выше которого должна
идти запись/логирование, и хочет: 1) снизить порог до 100; 2) чтобы по
этому же триггеру включалось всё, связанное с камерами (кроме самого
подключения — оно должно быть всегда включено, с автопереподключением,
даже если понадобится reboot).

**Важная находка:** такого порога сейчас НЕТ вообще нигде в коде (ни в
`magma_server.cpp`, ни в `.ino`, ни в клиентах) — вещание и логирование
идут всегда, безусловно. Более того, АЦП на Arduino **10-битный, диапазон
0..1023** (см. `magma_speed.ino:3`) — значение «5000» физически не может
быть порогом на этом АЦП (в 5 раз больше максимума). Порог 100 из 1023
(~10%) вменяемый, реализовать можно.

Заданные пользователю, но не отвеченные вопросы (нужно уточнить перед
реализацией):
1. По какому из 4 каналов АЦП считать «расплав присутствует» — любой канал
   выше порога, среднее по 4, или все 4 сразу?
2. Как камере (процесс `magma_vision`, отдельный от `magma_server`) узнать
   о срабатывании этого триггера — варианты: (а) `magma_vision` сам
   подключается как клиент к 127.0.0.1:9000 и читает `adc` из JSON
   (просто, но создаёт зависимость от того, что сервер запущен); (б) через
   файл состояния (`/run/magma-melt` или похожий), который пишет server
   и периодически читает vision — разделяет процессы полностью, но
   добавляет файловый обмен вместо сетевого.

Это правка ядра (`magma_server.cpp`) — трогать только с точным пониманием
семантики порога, поэтому не начинал реализацию без ответа на вопросы выше.

### Git на трёх машинах (обновлено 2026-09-16)

Кроме ПК (`mikeg`, машина `MIKE-HOME`) и Pi — репозиторий также клонирован
на ВТОРОЙ компьютер пользователя (пользователь Windows `M.Gusev`, другая
физическая машина) в `C:\Users\M.Gusev\Documents\magma-flow`, с тем же
SSH-ключом-паттерном (`~/.ssh/id_ed25519_github`, свой отдельный ключ для
этой машины, добавлен на GitHub). `git pull`/`push` там подтверждённо
работают без пароля.

**ВАЖНО для новой сессии, стартующей на этой странице:** сессии Claude Code
привязаны к машине, на которой были СОЗДАНЫ — открытие desktop-приложения
на другом физическом компьютере и продолжение того же чата НЕ переносит
инструменты (PowerShell и т.п.) на эту машину, они остаются на исходной.
Чтобы получить консоль машины `M.Gusev`, нужно начать НОВЫЙ чат, запущенный
локально на ней (это как раз то, что происходит при передаче этого файла).

## 10. Текущая незавершённая задача

Пользователь хочет настроить **вторую Raspberry Pi 5** проекта (роль пока
не подтверждена — см. п.8, вероятно клиентская/наблюдательная) из сессии,
запущенной на машине `M.Gusev`. Ещё не начато: нет ни SSH-доступа к второй
Pi, ни подтверждённой её роли, ни IP-адреса.

## 11. Что приложить в новый чат

Этот файл + любые вопросы из раздела 9 (порог АЦП) — пользователь ещё не
ответил на них, дальше по коду ничего не делать, пока не уточнит.
Остальные файлы проекта не нужно прикладывать вручную — репозиторий уже
клонирован на машине M.Gusev в `C:\Users\M.Gusev\Documents\magma-flow`
(`git pull` подтягивает всё актуальное).
